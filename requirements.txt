# Core dependencies
# edge-tts: Both providers use system edge-tts installation
#   - Main provider (edge_tts): Standard API method
#   - Working provider (edge_tts_working): Hugging Face demo's working API method (standalone)
edge-tts>=7.2.0
pyttsx3>=2.90  # Offline TTS provider
PySide6>=6.6.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Optional but recommended
playwright>=1.40.0  # For advanced scraping with JavaScript
pydub>=0.25.1  # For audio processing

# Audio format support
mutagen>=1.47.0  # For metadata handling in audio files





